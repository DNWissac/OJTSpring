<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ojt.movie.mapper.UserMapper">

    <select id="signIn" resultType="com.ojt.movie.model.dto.UserDto">
        SELECT
            sUserEmail,
            sPassword,
            sNickName,
            nUserAdmin
        FROM
            tUserList
        WHERE
            sUserEmail = #{sUserEmail}
        AND
            sUserPassword = #{sUserPassword}
    </select>

    <insert id="signUp">
        SELECT
            COUNT(*) AS COUNT
        FROM
            tMovieList
    </insert>

</mapper>